# ── Core data handling ────────────────────────────────────────────────────────
pandas>=2.0.0            # DataFrame operations; Parquet I/O via pyarrow backend
numpy>=1.26.0            # Numerical computation; log returns, rolling math
pyarrow>=14.0.0          # Parquet serialisation for all processed data caches

# ── Commodity price data ──────────────────────────────────────────────────────
yfinance>=0.2.40         # Yahoo Finance OHLCV fetcher — primary commodity source
pandas-datareader>=0.10.0 # Fallback data source (FRED, Quandl) if yfinance degrades

# ── GDELT HTTP downloads ──────────────────────────────────────────────────────
requests>=2.31.0         # Master file list fetch + CSV.zip downloads
tenacity>=8.2.0          # Exponential backoff decorator for HTTP retries

# ── Statistical analysis ──────────────────────────────────────────────────────
statsmodels>=0.14.0      # grangercausalitytests, adfuller, kpss, VAR lag selection
scipy>=1.12.0            # Pearson/Spearman correlations; FDR multiple-testing correction

# ── Progress & logging ────────────────────────────────────────────────────────
tqdm>=4.66.0             # Progress bars for batch GDELT file downloads

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.8.0        # Static chart generation embedded in HTML report
plotly>=5.20.0           # Interactive CCF heatmaps and timeline charts

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0            # Unit test runner
